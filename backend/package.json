{
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "seed": "ts-node-dev --transpile-only src/seeders/vehicleSeeder.ts"
  },
  "dependencies": {
    "express-validator": "^7.3.2"
  }
}
